[pytest]
pythonpath = .
asyncio_mode = auto

# Options de coverage (optionnel - peut aussi être lancé via ligne de commande)
# Pour activer automatiquement, décommentez les lignes suivantes:
# addopts = 
#     --cov=src
#     --cov=code
#     --cov-report=term-missing
#     --cov-report=html
#     -v